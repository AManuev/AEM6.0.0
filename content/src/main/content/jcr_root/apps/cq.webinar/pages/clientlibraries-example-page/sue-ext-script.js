use(['sue-script.js'], function (component) {

    component.title = component.title + ' Ext';
    component.description = component.description + ' Ext';
    component.specification = 'Specification Ext';

    return component;
});